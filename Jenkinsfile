pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                script {
                    sh 'sudo docker run hello-world'
                }
            }
        }
    }
}

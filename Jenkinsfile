pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                script {
                    sh 'docker run hello-world'
                }
            }
        }
    }
}
